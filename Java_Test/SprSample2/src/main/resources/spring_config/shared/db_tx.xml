<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx" xsi:schemaLocation="http://www.springframework.org/schema/beans 
						http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/aop 
						http://www.springframework.org/schema/aop/spring-aop.xsd
						http://www.springframework.org/schema/tx 
						http://www.springframework.org/schema/tx/spring-tx.xsd">

	<!-- Omit attribute "transaction-manager", then by default it will autowired in a bean named "transactionManager". -->
	<!-- If the name of bean is such as "txManager" other than "transactionManager", we should define attribute transaction-manager="txManager". -->
	<tx:advice id="txAdvice">
		<tx:attributes>
			<tx:method name="save" propagation="REQUIRES_NEW" />
			<tx:method name="query" propagation="SUPPORTS" read-only="true" />
		</tx:attributes>
	</tx:advice>

	<aop:config>
		<aop:advisor pointcut="execution(* *..ItemDao.*(..))" advice-ref="txAdvice" />

		<aop:advisor pointcut="execution(* *..ItemLogDao.*(..))" advice-ref="txAdvice" />
	</aop:config>

</beans>
